// In those particular situations where I want/need
// to push values from PHP to JAVASCRIPT using
// wp_localize_script, I am using this module
// to do the trick.

// Unfortunately, I must instruct everyone to localize
// their values to ctns-app, which in turn pushes these
// values onto 'localize'.
//
var LOCALIZE = {};
    
export default LOCALIZE;
